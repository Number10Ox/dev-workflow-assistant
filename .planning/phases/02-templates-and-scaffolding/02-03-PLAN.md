---
phase: 02-templates-and-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - skills/dwa-draft-tdd/SKILL.md
autonomous: true

must_haves:
  truths:
    - "User can invoke /dwa:draft-tdd to create a TDD"
    - "Skill checks for existing spec before proceeding"
    - "Skill warns before overwriting existing TDD"
    - "Skill provides clear next steps after creation"
  artifacts:
    - path: "skills/dwa-draft-tdd/SKILL.md"
      provides: "Claude Code skill for TDD drafting"
      contains: "name: draft-tdd"
  key_links:
    - from: "skills/dwa-draft-tdd/SKILL.md"
      to: "src/scaffolding/scaffold-tdd.js"
      via: "require statement"
      pattern: "scaffold-tdd"
    - from: "skills/dwa-draft-tdd/SKILL.md"
      to: "src/scaffolding/check-existing.js"
      via: "require statement"
      pattern: "check-existing"
---

<objective>
Create the /dwa:draft-tdd Claude Code skill for scaffolding Technical Design Documents.

Purpose: Enable users to invoke /dwa:draft-tdd in Claude Code to create a TDD linked to their existing feature spec. The skill follows the same 4-step pattern as /dwa:create-spec for consistent UX.
Output: SKILL.md file that instructs Claude how to check prerequisites, warn about existing files, scaffold the TDD, and confirm success.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-templates-and-scaffolding/02-RESEARCH.md
@.planning/phases/02-templates-and-scaffolding/02-02-SUMMARY.md

# Pattern to follow
@skills/dwa-create-spec/SKILL.md

# Utilities the skill will use
@src/scaffolding/scaffold-tdd.js
@src/scaffolding/check-existing.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dwa-draft-tdd skill directory and SKILL.md</name>
  <files>skills/dwa-draft-tdd/SKILL.md</files>
  <action>
Create skills/dwa-draft-tdd/SKILL.md following the same pattern as dwa-create-spec/SKILL.md:

```markdown
---
name: draft-tdd
description: Draft a Technical Design Document from an existing feature spec. Creates docs/tdds/<feature>-tdd.md with decision log, guardrails, and risks.
disable-model-invocation: true
argument-hint: [optional-tdd-filename]
---

# DWA: Draft Technical Design Document

## Purpose
Generate a TDD outline from the feature spec's goals, constraints, and deliverables. The TDD captures architecture decisions, guardrails, risks, and test strategy in a structured format linked to the feature spec.

## Process

### Step 1: Check prerequisites
Verify the feature spec exists before proceeding:

\`\`\`javascript
const { checkExisting } = require('[DWA_INSTALL_PATH]/src/scaffolding/check-existing');
const fs = require('fs-extra');
const path = require('path');

// Check spec and registry exist
const result = await checkExisting(process.cwd());
const specPath = path.join(process.cwd(), 'feature-spec.md');

if (!result.files.spec) {
  throw new Error('No feature spec found. Run /dwa:create-spec first.');
}

if (!result.files.featureJson) {
  throw new Error('No feature registry found. Run /dwa:create-spec first.');
}
\`\`\`

If either check fails, tell the user to run `/dwa:create-spec` first and stop.

### Step 2: Check for existing TDD
Check if a TDD already exists for this feature:

\`\`\`javascript
const featureJson = await fs.readJson(path.join(process.cwd(), '.dwa', 'feature.json'));

if (featureJson.tdd_path) {
  const tddExists = await fs.pathExists(path.join(process.cwd(), featureJson.tdd_path));
  if (tddExists) {
    // TDD already exists - ask before overwriting
  }
}

// Also check docs/tdds/ directory for any TDD files
const tddsDir = path.join(process.cwd(), 'docs', 'tdds');
if (await fs.pathExists(tddsDir)) {
  const tddFiles = await fs.readdir(tddsDir);
  if (tddFiles.some(f => f.endsWith('-tdd.md'))) {
    // TDD file exists - check if it matches this feature
  }
}
\`\`\`

If TDD already exists:
- Tell the user which TDD file already exists
- Ask: "TDD already exists at [path]. Overwrite? (y/n)"
- If user says no, stop with message: "Creation cancelled. Existing TDD preserved."
- If user says yes, continue to Step 3

### Step 3: Read spec context
Parse the feature spec to get metadata for the TDD:

\`\`\`javascript
const matter = require('gray-matter');
const specContent = await fs.readFile(specPath, 'utf8');
const { data } = matter(specContent);

const featureId = data.feature_id;
const featureTitle = data.title;
\`\`\`

### Step 4: Scaffold TDD
Run the scaffold utility to create the TDD:

\`\`\`javascript
const { scaffoldTDD } = require('[DWA_INSTALL_PATH]/src/scaffolding/scaffold-tdd');

const tddPath = await scaffoldTDD(
  featureId,
  featureTitle,
  'feature-spec.md',
  process.cwd()
);
\`\`\`

This creates:
- `docs/tdds/<feature-slug>-tdd.md` - Technical Design Document with all sections
- Updates `.dwa/feature.json` with `tdd_path` field
- Updates `feature-spec.md` frontmatter with `tdd_path` field

### Step 5: Confirm success
Report to user:
- "TDD created: [tddPath]"
- "Feature registry updated: .dwa/feature.json"
- "Spec linked: feature-spec.md frontmatter now includes tdd_path"
- ""
- "Next steps:"
- "1. Review Objectives section - align with spec goals"
- "2. Fill in Architecture Overview - diagram or description"
- "3. Document key decisions in Decision Log using ADR format"
- "4. Define Guardrails - performance, security, compatibility constraints"
- "5. Identify Risks and mitigation strategies"
- "6. Outline Test Strategy with evidence requirements"

## Notes
- [DWA_INSTALL_PATH] resolves to the DWA package installation directory (where npx dwa was installed from)
- The scaffold utility uses templates/tdd-v1.hbs template
- TDD files go in docs/tdds/ (not .dwa/) because they are technical documentation meant for version control
- Bidirectional linking: TDD contains spec_path, spec contains tdd_path, feature.json contains both
- ADR format (Architecture Decision Record) is used for the Decision Log section
```
  </action>
  <verify>
Read skills/dwa-draft-tdd/SKILL.md and confirm:
- Frontmatter has name: draft-tdd
- 5-step process documented (Check prerequisites, Check existing, Read spec, Scaffold, Confirm)
- Uses checkExisting from check-existing.js
- Uses scaffoldTDD from scaffold-tdd.js
- Warns before overwriting existing TDD
- Provides clear next steps
- Notes explain [DWA_INSTALL_PATH] placeholder
  </verify>
  <done>Skill file exists with complete 5-step process for TDD creation</done>
</task>

<task type="auto">
  <name>Task 2: Verify skill integration</name>
  <files>skills/dwa-draft-tdd/SKILL.md</files>
  <action>
Verify the skill file is correctly integrated:

1. Confirm directory structure matches other skills:
   ```
   skills/
   ├── dwa-create-spec/
   │   └── SKILL.md
   └── dwa-draft-tdd/
       └── SKILL.md
   ```

2. Verify the skill references the correct utility paths:
   - `[DWA_INSTALL_PATH]/src/scaffolding/check-existing`
   - `[DWA_INSTALL_PATH]/src/scaffolding/scaffold-tdd`

3. Verify the skill frontmatter matches the pattern:
   - name: draft-tdd
   - description: Draft a Technical Design Document...
   - disable-model-invocation: true
   - argument-hint: [optional-tdd-filename]

4. Ensure error messages are helpful:
   - "No feature spec found. Run /dwa:create-spec first."
   - "TDD already exists at [path]. Overwrite? (y/n)"
  </action>
  <verify>
- ls skills/ shows both dwa-create-spec/ and dwa-draft-tdd/
- Skill file contains all required sections
- References to scaffold-tdd.js and check-existing.js are correct
  </verify>
  <done>Skill is correctly structured and integrated with existing skill pattern</done>
</task>

</tasks>

<verification>
1. Skill file exists at skills/dwa-draft-tdd/SKILL.md
2. Skill frontmatter is valid:
   ```bash
   head -6 skills/dwa-draft-tdd/SKILL.md
   ```
   Should show:
   ```
   ---
   name: draft-tdd
   description: Draft a Technical Design Document...
   disable-model-invocation: true
   argument-hint: [optional-tdd-filename]
   ---
   ```
3. Skill references correct utilities (grep for require statements)
4. Skill has all 5 steps documented
5. Skill provides clear next steps for user
</verification>

<success_criteria>
- skills/dwa-draft-tdd/SKILL.md exists with valid frontmatter
- Skill follows same pattern as dwa-create-spec (5-step process)
- Step 1 checks prerequisites (spec + feature.json must exist)
- Step 2 warns before overwriting existing TDD
- Step 3 reads spec context for metadata
- Step 4 calls scaffoldTDD utility
- Step 5 confirms success with next steps
- [DWA_INSTALL_PATH] placeholder used for utility paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates-and-scaffolding/02-03-SUMMARY.md`
</output>
