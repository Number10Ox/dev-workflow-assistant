---
phase: 02-templates-and-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - skills/dwa-init/SKILL.md
  - tests/scaffold.test.js
autonomous: true

must_haves:
  truths:
    - "/dwa:init skill file instructs Claude to scaffold a feature spec from template"
    - "/dwa:init skill warns before overwriting existing spec"
    - "scaffoldFromTemplate produces valid YAML frontmatter parseable by gray-matter"
    - "scaffoldFromTemplate creates feature.json with schemaVersion field"
    - "checkExisting returns true when feature-spec.md or .dwa/feature.json exists"
  artifacts:
    - path: "skills/dwa-init/SKILL.md"
      provides: "/dwa:init Claude Code skill"
      contains: "scaffoldFromTemplate"
    - path: "tests/scaffold.test.js"
      provides: "Tests for scaffold and check-existing utilities"
      min_lines: 60
  key_links:
    - from: "skills/dwa-init/SKILL.md"
      to: "src/scaffolding/scaffold.js"
      via: "instructs Claude to require and call scaffoldFromTemplate"
      pattern: "scaffoldFromTemplate"
    - from: "skills/dwa-init/SKILL.md"
      to: "src/scaffolding/check-existing.js"
      via: "instructs Claude to require and call checkExisting"
      pattern: "checkExisting"
    - from: "tests/scaffold.test.js"
      to: "src/scaffolding/scaffold.js"
      via: "require and invoke"
      pattern: "require.*scaffold"
---

<objective>
Create the /dwa:init Claude Code skill file and tests for the scaffold utilities.

Purpose: The SKILL.md is what Claude reads when a user invokes `/dwa:init`. Tests verify the utilities produce correct output. Together they complete the Phase 2 deliverable.
Output: skills/dwa-init/SKILL.md, tests/scaffold.test.js
</objective>

<execution_context>
@/Users/jedwards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jedwards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-templates-and-scaffolding/02-01-SUMMARY.md
@src/scaffolding/scaffold.js
@src/scaffolding/check-existing.js
@tests/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /dwa:init SKILL.md</name>
  <files>skills/dwa-init/SKILL.md</files>
  <action>
Create the directory `skills/dwa-init/` and write `SKILL.md` inside it.

The skill file is a markdown document with YAML frontmatter that Claude reads as instructions. It is NOT executable code - it tells Claude what to do step by step.

Structure:

```markdown
---
name: init
description: Initialize a new feature spec from Template v2.0. Creates feature-spec.md and .dwa/feature.json in the current directory.
disable-model-invocation: true
argument-hint: [feature-title]
---

# DWA: Initialize Feature Spec

## Purpose
Scaffold a new feature spec from Template v2.0, creating `feature-spec.md` with YAML front matter and an empty Deliverables Table, plus `.dwa/feature.json` with feature metadata.

## Process

### Step 1: Check for existing initialization
Run the check-existing utility to see if this directory already has a feature spec:

\`\`\`javascript
const { checkExisting } = require('[DWA_INSTALL_PATH]/src/scaffolding/check-existing');
const result = await checkExisting(process.cwd());
\`\`\`

If `result.alreadyInitialized` is true:
- Tell the user which files already exist (feature-spec.md, .dwa/feature.json, or both)
- Ask: "Feature spec already exists. Overwrite? (y/n)"
- If user says no, stop with message: "Initialization cancelled. Existing files preserved."
- If user says yes, continue to Step 2

### Step 2: Get feature title
If a feature title was provided as an argument, use it.
Otherwise, ask the user: "What is the feature title?"

The title will be used in:
- YAML frontmatter `title` field
- H1 heading in the spec
- `title` field in .dwa/feature.json

### Step 3: Scaffold the feature spec
Run the scaffold utility:

\`\`\`javascript
const { scaffoldFromTemplate } = require('[DWA_INSTALL_PATH]/src/scaffolding/scaffold');
const result = await scaffoldFromTemplate(featureTitle, process.cwd());
\`\`\`

This creates:
- `feature-spec.md` - Feature spec with YAML front matter and empty Deliverables Table
- `.dwa/feature.json` - Feature metadata with schemaVersion, title, spec_path, created_at

### Step 4: Confirm success
Report to user:
- "Feature spec initialized: feature-spec.md"
- "Feature registry created: .dwa/feature.json"
- ""
- "Next steps:"
- "1. Edit feature-spec.md to fill in Overview, User Stories, and Deliverables Table"
- "2. Run /dwa:parse to extract deliverables into the registry"

## Notes
- [DWA_INSTALL_PATH] resolves to the DWA package installation directory (where npx dwa was installed from)
- The scaffold utility uses the package's templates/feature-spec-v2.hbs template
- feature.json always includes schemaVersion field via writeJsonWithSchema utility
- Google Docs import is planned for Phase 8 (not yet implemented)
```

Key requirements for the SKILL.md:
- YAML frontmatter: name, description, disable-model-invocation: true, argument-hint
- Clear step-by-step instructions Claude can follow
- References to actual utility functions with require paths
- Overwrite protection (Step 1) before scaffolding (Step 3)
- Uses `[DWA_INSTALL_PATH]` as placeholder - Claude resolves this to actual path at runtime
- Remove the .gitkeep placeholder from skills/ since it now has real content
  </action>
  <verify>
- `cat skills/dwa-init/SKILL.md` shows valid YAML frontmatter with name: init
- File contains references to checkExisting and scaffoldFromTemplate
- File has 4 steps: check existing, get title, scaffold, confirm
- File mentions overwrite warning
  </verify>
  <done>skills/dwa-init/SKILL.md exists with valid Claude Code skill format. Contains YAML frontmatter (name: init, disable-model-invocation: true). Instructions reference checkExisting and scaffoldFromTemplate utilities. Includes overwrite protection flow.</done>
</task>

<task type="auto">
  <name>Task 2: Create scaffold utility tests</name>
  <files>tests/scaffold.test.js</files>
  <action>
Create `tests/scaffold.test.js` using node:test (matching Phase 1 test patterns). Use DWA_TEST_HOME for test isolation.

Tests to write:

1. **scaffoldFromTemplate - creates feature-spec.md with valid content**
   - Set DWA_TEST_HOME to a temp directory
   - Call scaffoldFromTemplate('Test Feature', targetDir)
   - Assert feature-spec.md exists in targetDir
   - Assert content contains YAML frontmatter (starts with ---)
   - Assert content contains `title: "Test Feature"`
   - Assert content contains `# Test Feature` heading
   - Assert content contains the Deliverables Table header row (| ID | Title | ...)

2. **scaffoldFromTemplate - creates .dwa/feature.json with schema**
   - Call scaffoldFromTemplate('My Feature', targetDir)
   - Assert .dwa/feature.json exists
   - Parse JSON and assert:
     - schemaVersion is "1.0.0"
     - title is "My Feature"
     - spec_path is "feature-spec.md"
     - created_at is a valid ISO date string

3. **scaffoldFromTemplate - no unreplaced placeholders**
   - Call scaffoldFromTemplate('Feature: With Special Chars & Stuff', targetDir)
   - Read feature-spec.md content
   - Assert content does NOT match /\{\{[^}]+\}\}/ (no Handlebars placeholders remain)

4. **checkExisting - returns false for empty directory**
   - Call checkExisting(emptyTempDir)
   - Assert result.alreadyInitialized is false
   - Assert result.files.featureJson is false
   - Assert result.files.spec is false

5. **checkExisting - detects feature-spec.md**
   - Create feature-spec.md in targetDir
   - Call checkExisting(targetDir)
   - Assert result.alreadyInitialized is true
   - Assert result.files.spec is true

6. **checkExisting - detects .dwa/feature.json**
   - Create .dwa/feature.json in targetDir
   - Call checkExisting(targetDir)
   - Assert result.alreadyInitialized is true
   - Assert result.files.featureJson is true

Test setup pattern (from Phase 1):
```javascript
const { describe, it, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs-extra');
const path = require('node:path');
const os = require('node:os');

// Create isolated temp dir for each test
let testDir;
beforeEach(async () => {
  testDir = await fs.mkdtemp(path.join(os.tmpdir(), 'dwa-test-'));
});
afterEach(async () => {
  await fs.remove(testDir);
});
```

Important: Do NOT set process.env.DWA_TEST_HOME in these tests since scaffold.js uses the targetDir parameter directly (not homeDir). The temp dir isolation via parameters is sufficient.
  </action>
  <verify>
- `node --test tests/scaffold.test.js` passes all 6 tests
- `npm test` passes (all tests including Phase 1 tests)
  </verify>
  <done>tests/scaffold.test.js has 6 passing tests covering scaffoldFromTemplate (file creation, JSON schema, placeholder safety) and checkExisting (empty dir, spec exists, feature.json exists). All existing Phase 1 tests still pass.</done>
</task>

</tasks>

<verification>
- `npm test` passes all tests (Phase 1 + Phase 2)
- skills/dwa-init/SKILL.md exists with proper Claude Code skill format
- The skill references the correct utility functions
- Tests verify the core behaviors: spec creation, JSON schema, overwrite detection
</verification>

<success_criteria>
- skills/dwa-init/SKILL.md has valid YAML frontmatter (name: init, disable-model-invocation: true)
- SKILL.md instructs Claude to check existing, get title, scaffold, confirm
- tests/scaffold.test.js has 6 tests all passing
- Tests prove: valid YAML frontmatter in spec, schemaVersion in JSON, no placeholder leakage, correct checkExisting behavior
- `npm test` passes all tests (9 from Phase 1 + 6 from Phase 2 = 15 total)
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates-and-scaffolding/02-02-SUMMARY.md`
</output>
