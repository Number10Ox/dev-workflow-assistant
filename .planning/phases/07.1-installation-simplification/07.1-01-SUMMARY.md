---
phase: 07.1-installation-simplification
plan: 01
subsystem: tooling
tags: [cli, setup, feature-detection, enquirer, onboarding]

# Dependency graph
requires:
  - phase: 01-bootstrap-and-installer
    provides: Core CLI structure and install command
  - phase: 06-linear-integration
    provides: sync-linear command requiring Linear extension
  - phase: 07-polish-and-extended-features
    provides: import-gdoc command requiring Google Workspace extension

provides:
  - Interactive setup wizard for optional features (Linear, Google Docs)
  - Status command showing installation and feature configuration
  - Graceful degradation with clear setup instructions
  - Feature detection utility for runtime capability checks
  - Zero-prompt installation (npx dwa --install works immediately)

affects: [07.2-workflow-friction-reduction, future-extensions]

# Tech tracking
tech-stack:
  added: [enquirer]
  patterns:
    - Feature detection with graceful degradation
    - Interactive CLI wizards with enquirer MultiSelect
    - VS Code extension installation via CLI

key-files:
  created:
    - src/utils/feature-detection.js
    - src/commands/setup.js
    - src/commands/status.js
    - tests/utils/feature-detection.test.js
    - tests/commands/setup.test.js
    - tests/commands/status.test.js
  modified:
    - package.json
    - package-lock.json
    - src/cli.js
    - src/commands/sync-linear.js
    - src/commands/import-gdoc.js

key-decisions:
  - "Use enquirer for interactive prompts (established pattern for wizard UIs)"
  - "Feature detection via VS Code extension checks (graceful in CLI context)"
  - "Separate --setup and --status from core operations (clear separation of concerns)"
  - "Automatic extension installation via code CLI when available"

patterns-established:
  - "Feature detection pattern: checkFeature() returns {available, reason} for runtime checks"
  - "Setup instructions pattern: clear multi-step guidance with manual fallback"
  - "Graceful degradation: show helpful error messages, not stack traces"

# Metrics
duration: 5m 6s
completed: 2026-01-26
---

# Phase 07.1 Plan 01: Installation Simplification Summary

**Zero-prompt installation with interactive setup wizard and graceful feature detection for optional Linear/Google Docs integrations**

## Performance

- **Duration:** 5 min 6 sec
- **Started:** 2026-01-26T05:15:47Z
- **Completed:** 2026-01-26T05:20:53Z
- **Tasks:** 4
- **Files modified:** 12

## Accomplishments

- Interactive setup wizard with enquirer for guided feature configuration
- Status command showing installation state and feature availability
- Graceful degradation in sync-linear and import-gdoc (show setup instructions, not errors)
- Feature detection utility with VS Code extension checks
- All 399 tests passing (7 new tests added)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add enquirer dependency and feature detection utility** - `913979c` (feat)
   - Added enquirer npm package
   - Created feature-detection.js with checkFeature(), getSetupInstructions(), getAllFeatureStatus()
   - Full test coverage for feature detection

2. **Task 2: Create setup wizard command** - `5c52812` (feat)
   - Interactive wizard with MultiSelect for feature selection
   - Direct setup flags: --setup linear, --setup google-docs
   - Automatic VS Code extension installation via code CLI
   - Clear next-step instructions after install

3. **Task 3: Create status command** - `d5ffdfe` (feat)
   - Shows installation status with version
   - Lists core features (always available)
   - Lists optional features with availability indicators
   - Setup instructions for missing features

4. **Task 4: Integrate into CLI and update existing commands** - `dfb76ae` (feat)
   - Added --setup and --status to CLI options
   - Mutual exclusivity checks for new operations
   - Graceful degradation in sync-linear (check Linear feature)
   - Graceful degradation in import-gdoc (check Google Docs feature)
   - Help text updated with new options

## Files Created/Modified

**Created:**
- `src/utils/feature-detection.js` - Runtime feature availability checks and setup instructions
- `src/commands/setup.js` - Interactive wizard and direct feature setup
- `src/commands/status.js` - Installation and feature status display
- `tests/utils/feature-detection.test.js` - Feature detection test suite
- `tests/commands/setup.test.js` - Setup command test suite
- `tests/commands/status.test.js` - Status command test suite

**Modified:**
- `package.json` - Added enquirer dependency
- `src/cli.js` - Integrated --setup and --status options
- `src/commands/sync-linear.js` - Added feature detection check
- `src/commands/import-gdoc.js` - Added feature detection check

## Decisions Made

1. **Use enquirer for interactive prompts** - Provides rich CLI interactions with MultiSelect, consistent with modern CLI tools
2. **Feature detection via VS Code extension checks** - Gracefully handles both VS Code and CLI contexts
3. **Separate --setup and --status operations** - Clear separation from core install/upgrade operations
4. **Auto-install extensions via code CLI** - When available, automatic; otherwise clear manual instructions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. All tasks completed successfully with no blocking issues.

## User Setup Required

None - no external service configuration required.

The setup wizard itself provides guided configuration for users wanting optional features.

## Next Phase Readiness

- Installation friction reduced from multi-step to single command
- Setup wizard ready for demo and onboarding flows
- Graceful degradation ensures commands never crash from missing features
- Status command provides clear visibility into configuration state
- Ready for Phase 07.2 workflow friction reduction

**Blockers/concerns:** None

---
*Phase: 07.1-installation-simplification*
*Completed: 2026-01-26*
