---
phase: 06-linear-integration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Users can invoke sync command via 'dwa --sync-linear'"
    - "Users can preview sync with '--dry-run' flag"
    - "Users can force overwrite conflicts with '--force' flag"
    - "Users can sync specific deliverables with '--deliverables DEL-001,DEL-002'"
    - "Users can specify Linear project with '--project' flag"
  artifacts:
    - path: "src/cli.js"
      provides: "CLI entry point with --sync-linear routing"
      contains: ".option('--sync-linear'"
  key_links:
    - from: "src/cli.js"
      to: "src/commands/sync-linear.js"
      via: "require and syncLinear() call"
      pattern: "require.*sync-linear.*syncLinear"
---

<objective>
Wire the sync-linear command into the CLI entry point.

Purpose: Close the gap identified in verification - the sync-linear.js command implementation is complete but users cannot invoke it because cli.js lacks the --sync-linear option and routing logic.

Output: Users can run `dwa --sync-linear` with all sub-options (--dry-run, --force, --deliverables, --project)
</objective>

<execution_context>
@/Users/jedwards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jedwards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-linear-integration/06-VERIFICATION.md

# Existing implementation to wire in
@src/commands/sync-linear.js
@src/cli.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --sync-linear option and routing to CLI</name>
  <files>src/cli.js</files>
  <action>
Add the --sync-linear option and its sub-options to the commander program definition, then add routing logic to call the syncLinear function.

1. Add options after the existing --uninstall option:
   - `.option('--sync-linear', 'Sync deliverables to Linear')`
   - `.option('--dry-run', 'Preview sync without making changes (requires --sync-linear)')`
   - `.option('--force', 'Overwrite DWA sections even if manually edited (requires --sync-linear)')`
   - `.option('--deliverables <ids>', 'Comma-separated deliverable IDs to sync (requires --sync-linear)')`
   - `.option('--project <id>', 'Linear project ID or URL (requires --sync-linear)')`

2. Update mutual exclusivity check to include sync-linear:
   - Add `opts.syncLinear` to the operation count check
   - Note: --dry-run, --force, --deliverables, --project are NOT operations themselves, they are modifiers for --sync-linear

3. Add routing logic for --sync-linear after the existing uninstall block:
```javascript
} else if (opts.syncLinear) {
  const { syncLinear } = require('./commands/sync-linear');
  const result = await syncLinear({
    projectRoot: process.cwd(),
    dryRun: opts.dryRun,
    force: opts.force,
    deliverables: opts.deliverables,
    project: opts.project
  });
  if (!result.success) {
    console.error(result.message);
    process.exit(1);
  }
  console.log(result.message);
}
```

4. Since syncLinear is async, wrap the routing logic in an async IIFE:
   - The existing install/upgrade/uninstall handlers are sync, but sync-linear is async
   - Wrap the entire routing section in `(async () => { ... })().catch(err => { console.error(err); process.exit(1); });`
   - OR make just the sync-linear block use `.then()/.catch()` pattern

5. Keep error handling consistent with existing patterns - use try/catch with MODULE_NOT_FOUND check for graceful degradation.

Note: Commander.js uses camelCase for multi-word options (--sync-linear becomes opts.syncLinear, --dry-run becomes opts.dryRun).
  </action>
  <verify>
Run `node src/cli.js --help` and verify:
- `--sync-linear` option appears in help
- `--dry-run`, `--force`, `--deliverables`, `--project` options appear
- No syntax errors

Run `node src/cli.js --sync-linear --dry-run` in a directory without .dwa/:
- Should fail gracefully with "No feature found" message (not a crash)
  </verify>
  <done>
- `dwa --sync-linear` invokes the syncLinear function
- Sub-options (--dry-run, --force, --deliverables, --project) pass through to syncLinear
- Mutual exclusivity prevents --sync-linear with --install/--upgrade/--uninstall
- Error handling matches existing CLI patterns
  </done>
</task>

</tasks>

<verification>
1. Run `node src/cli.js --help` - all sync options visible
2. Run `node src/cli.js --sync-linear --install` - fails with mutual exclusivity error
3. Run `node src/cli.js --sync-linear` in non-DWA project - fails gracefully with "No feature found"
4. Run `npm test` - all existing tests still pass
</verification>

<success_criteria>
- [x] --sync-linear option wired into CLI
- [x] Sub-options pass through correctly
- [x] Mutual exclusivity enforced
- [x] Error handling consistent with existing patterns
- [x] Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-linear-integration/06-05-SUMMARY.md`
</output>
